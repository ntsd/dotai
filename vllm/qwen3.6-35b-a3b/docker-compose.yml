# Credit: https://github.com/AEON-7/Qwen3.6-NVFP4-DFlash
#
# Qwen3.6-35B-A3B-heretic NVFP4 + DFlash on DGX Spark
#
# Production config validated 2026-04-20.
#   - Image: ghcr.io/aeon-7/vllm-spark-omni-q36:v1.2 (or :latest)
#   - Model: AEON-7/Qwen3.6-35B-A3B-heretic-NVFP4 (v2 multimodal-preserved, ~22 GB)
#   - Drafter: z-lab/Qwen3.6-35B-A3B-DFlash (~905 MB; MUST be re-pulled if cloned before 2026-04-19)
#
# Measured performance (single-stream, greedy):
#   ~117 tok/s short prompts; ~785 tok/s aggregate at 128-concurrency.
#
# Default layout (override with QWEN36_MODELS_DIR=/your/path docker compose up -d):
#   /opt/qwen36/qwen36-nvfp4/    ← AEON-7/Qwen3.6-35B-A3B-heretic-NVFP4
#   /opt/qwen36/qwen36-dflash/   ← z-lab/Qwen3.6-35B-A3B-DFlash
#
# Required downloads (run once):
#   hf auth login
#   hf download AEON-7/Qwen3.6-35B-A3B-heretic-NVFP4 --local-dir ./models/qwen36-nvfp4
#   hf download z-lab/Qwen3.6-35B-A3B-DFlash --local-dir ./models/qwen36-dflash
#
# Start:
#   docker compose up -d
#   docker compose logs -f vllm

services:
  vllm:
    image: ghcr.io/aeon-7/vllm-spark-omni-q36:v1.2
    container_name: vllm-qwen36-heretic
    restart: unless-stopped
    network_mode: host
    environment:
      - VLLM_ALLOW_LONG_MAX_MODEL_LEN=1
      - TORCH_MATMUL_PRECISION=high
      - PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
      - NVIDIA_FORWARD_COMPAT=1
      # ▼ Belt-and-suspenders for NVFP4 MoE backend selection ▼
      # vLLM's auto-selector already arrives at MARLIN for the MoE path on this
      # 256-expert × 512-intermediate shape (every other backend — FLASHINFER_TRTLLM,
      # FLASHINFER_CUTEDSL{,_BATCHED}, FLASHINFER_CUTLASS, VLLM_CUTLASS — rejects our
      # config in is_supported_config()). This env var pins the choice defensively
      # against future vLLM versions that might add a half-broken backend.
      #
      # IMPORTANT: this env affects MoE only. The LINEAR (q/k/v/o, mlp) path uses
      # FlashInferCutlassNvFp4LinearKernel — native FP4 tensor cores on SM121,
      # unaffected by this env, autotuned at boot. So we ARE getting native FP4
      # hardware on the linear layers; only the MoE expert path falls back to
      # Marlin's weight-only-decompress-to-BF16 kernel until kernels are widened.
      #
      # See docs/troubleshooting.md "Root cause clarification" for the full picture.
      - VLLM_TEST_FORCE_FP8_MARLIN=1
    volumes:
      - ./models/qwen36-nvfp4:/models/qwen36
      - ./models/qwen36-dflash:/models/qwen36-dflash
    command:
      - bash
      - -c
      - |
        exec vllm serve /models/qwen36 \
          --served-model-name qwen36-35b-heretic qwen36-fast qwen36-deep \
          --host 0.0.0.0 --port 8000 \
          --tensor-parallel-size 1 \
          --dtype auto \
          --quantization compressed-tensors \
          --max-model-len 262144 \
          --max-num-seqs 16 \
          --max-num-batched-tokens 65536 \
          --gpu-memory-utilization 0.85 \
          --enable-chunked-prefill \
          --enable-prefix-caching \
          --load-format safetensors \
          --trust-remote-code \
          --enable-auto-tool-choice \
          --tool-call-parser qwen3_coder \
          --reasoning-parser qwen3 \
          --speculative-config '{"method":"dflash","model":"/models/qwen36-dflash","num_speculative_tokens":15}' \
          --attention-backend flash_attn
          # NOTE: --enforce-eager is NOT required when using v1.2 image + the latest z-lab
          # DFlash drafter (re-pulled 2026-04-19+). The image's compilation.py:1378 patch
          # (auto-aligns CUDA graph capture sizes to multiples of 1+spec_tokens) plus the
          # drafter's long-context fix means CUDA graphs are stable and give ~30% perf
          # over eager mode.
    ipc: host
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
